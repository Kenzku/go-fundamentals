// go.mod indicates the current folder is a module
// this module is called demo
// a package is a folder insdie a module, with at least 1 source file
// all members are visiable to other package members (data are shared in the same folder)
// so module > pacakge
module demo

go 1.19

